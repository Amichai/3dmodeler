<Window x:Class="Renderer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" PreviewMouseLeftButtonDown="viewRoot_PreviewMouseLeftButtonDown" PreviewMouseLeftButtonUp="viewRoot_PreviewMouseLeftButtonUp" PreviewMouseMove="viewRoot_PreviewMouseMove" Background="Black">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid Name="viewRoot" Grid.Row="1">
        </Grid>
        <StackPanel VerticalAlignment="Center">
            <Button>Button1</Button>
            <Button>Button2</Button>
            <Button>Button3</Button>
        </StackPanel>
        <Slider Orientation="Vertical" Minimum="-100" Maximum="100" Grid.Column="2" Value="0"></Slider>
    </Grid>
</Window>
